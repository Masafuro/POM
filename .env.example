# =================================================================
# POM (Personalized Office Master) Environment Configuration
# =================================================================

# --- 1. POP3 Mail Server Settings ---
# メールサーバーの接続情報を設定します。パスワードは Git に含めないよう注意してください。
POP3_HOST=pop.example.com
POP3_PORT=995
POP3_USER=your_email@example.com
POP3_PASSWORD=your_app_specific_password

# --- 2. Database Settings ---
# SQLite データベースファイルの保存先を指定します。
# フォルダが存在しない場合は、起動時に自動作成されることを想定しています。
DATABASE_PATH=data/pom.db

# --- 3. Application Behavior ---
# システムの挙動を制御するフラグと数値の設定です。
POLLING_INTERVAL=600  # ポーリング間隔（秒）。600なら10分おき。
LOG_LEVEL=INFO        # DEBUG, INFO, WARNING, ERROR, CRITICAL
DEBUG_MODE=false      # true に設定すると、詳細なエラー情報が出力されます。

# --- 4. Security & Extension (Optional) ---
# 将来的に暗号化などを行う場合に使用する鍵のプレースホルダです。
SECRET_KEY=generate_a_random_string_here